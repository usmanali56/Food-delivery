# Paste these in Vercel → Project → Settings → Environment Variables (Production + Preview)

MONGO_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/food-delivery

# Customer frontend calls same-origin /api (set at build time)
VITE_API_URL=/api

# Optional: your main production URL (preview *.vercel.app URLs work automatically)
CLIENT_ORIGIN=https://food-delivery-zr4w.vercel.app
